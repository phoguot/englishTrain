<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 * @var string $content
 * @var bool|null $publicLanding
 */
$user = $this->currentUser();
$isPublicLanding = (bool) ($publicLanding ?? false);
$role = $user['role'] ?? null;
$roleLabels = [
    'admin'   => 'Quản trị',
    'teacher' => 'Giáo viên',
    'student' => 'Học sinh',
];
$spaceLabels = [
    'admin'   => 'Quản trị hệ thống',
    'teacher' => 'Không gian giáo viên',
    'student' => 'Không gian học sinh',
];
?><!doctype html>
<html lang="vi">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <title>EnglishTrain</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="<?= $this->basePath('css/app.css') ?>" rel="stylesheet">
</head>
<body<?= $role === 'student' && !$isPublicLanding ? ' data-role="student"' : '' ?>>
<?php if ($user === null || $isPublicLanding): ?>
    <?= $content ?>
<?php else: ?>
    <a class="skip-link" href="#noi-dung">Bỏ qua điều hướng</a>

    <header class="topbar">
        <a class="brand" href="<?= $this->url('dashboard') ?>">
            <span class="brand__mark" aria-hidden="true">ET</span>
            <span class="brand__name">EnglishTrain</span>
        </a>
        <div class="topbar__actions">
            <div class="user-chip">
                <span class="user-chip__monogram" aria-hidden="true">
                    <?= $this->escapeHtml(strtoupper(substr((string) $role, 0, 2))) ?>
                </span>
                <span>
                    <?= $this->escapeHtml($user['name']) ?> ·
                    <?= $this->escapeHtml($roleLabels[$role] ?? (string) $role) ?>
                </span>
            </div>
            <button class="btn btn--outline menu-button" type="button" data-menu-button
                    aria-controls="side-menu" aria-expanded="false">Menu</button>
        </div>
    </header>

    <div class="app-shell">
        <aside class="side-nav" id="side-menu" data-menu>
            <p class="side-nav__label"><?= $this->escapeHtml($spaceLabels[$role] ?? 'EnglishTrain') ?></p>
            <nav aria-label="Điều hướng chính">
                <ul class="side-nav__list">
                    <li>
                        <a class="side-nav__link" href="<?= $this->url('dashboard') ?>">
                            Tổng quan <span>01</span>
                        </a>
                    </li>
                    <?php if (in_array($role, ['admin', 'teacher'], true)): ?>
                        <li>
                            <a class="side-nav__link" href="<?= $this->url('classrooms') ?>">
                                Lớp học <span>02</span>
                            </a>
                        </li>
                    <?php endif; ?>
                    <?php if ($role === 'admin'): ?>
                        <li><a class="side-nav__link" href="<?= $this->url('users') ?>">Tài khoản <span>03</span></a></li>
                        <li><a class="side-nav__link" href="<?= $this->url('system_logs') ?>">Log hệ thống <span>04</span></a></li>
                    <?php endif; ?>
                    <?php if ($role === 'student'): ?>
                        <li>
                            <a class="side-nav__link"
                               href="<?= $this->url('attendance_student', ['id' => $user['id']]) ?>">
                                Chuyên cần <span>02</span>
                            </a>
                        </li>
                        <li>
                            <a class="side-nav__link" href="<?= $this->url('my_reports') ?>">
                                Report của tôi <span>03</span>
                            </a>
                        </li>
                    <?php endif; ?>
                    <li>
                        <form method="post" action="<?= $this->url('logout') ?>">
                            <input type="hidden" name="_csrf" value="<?= $this->escapeHtmlAttr($user['csrfToken']) ?>">
                            <button class="side-nav__link border-0 bg-transparent w-100 text-start" type="submit">
                                Đăng xuất <span>→</span>
                            </button>
                        </form>
                    </li>
                </ul>
            </nav>
            <p class="side-nav__meta">EnglishTrain Center<br>Học có nhịp. Dạy có dấu.</p>
        </aside>

        <main class="page" id="noi-dung">
            <?= $this->flashMessenger()->render('success', ['alert', 'alert-success']) ?>
            <?= $this->flashMessenger()->render('error', ['alert', 'alert-danger']) ?>
            <?= $content ?>

            <footer class="footer<?= $role === 'student' ? ' footer--statement' : '' ?>">
                <?php if ($role === 'student'): ?>
                    <strong>Học từng chút, giỏi thêm mỗi ngày.</strong>
                <?php endif; ?>
                <span>EnglishTrain · <?= $this->escapeHtml($spaceLabels[$role] ?? '') ?></span>
                <a href="<?= $this->url('dashboard') ?>">Về tổng quan</a>
            </footer>
        </main>
    </div>
<?php endif; ?>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="<?= $this->basePath('js/app.js') ?>"></script>
<script src="<?= $this->basePath('js/quiz-builder.js') ?>"></script>
<script src="<?= $this->basePath('js/report-editor.js') ?>"></script>
</body>
</html>
